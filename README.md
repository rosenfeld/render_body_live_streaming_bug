# Bug affecting Live streaming and "render body"

This is a sample application demonstrating a bug when using `render body` with
ActionController::Live.

Comment the `include` in `main_controller.rb` and it works.
